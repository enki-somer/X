[build]
  base = "Frontend"
  command = "npm install && npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "18" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_ENV = "production"
  NPM_FLAGS = "--no-audit --no-fund" 